# 🚨 СРОЧНОЕ ИСПРАВЛЕНИЕ - Бесконечная рекурсия в чате

## ❌ Ошибка
```
infinite recursion detected in policy for relation "support_messages"
```

---

## ⚡ БЫСТРОЕ РЕШЕНИЕ

### 1️⃣ Supabase Dashboard
https://supabase.com → SubManager → SQL Editor

### 2️⃣ Выполните скрипт
Файл: **`fix-support-messages-recursion.sql`**

Скопируйте → Вставьте → Run (Ctrl+Enter)

### 3️⃣ Готово!
Обновите страницу приложения (F5)

---

## 📄 SQL код (для быстрого копирования)

```sql
-- Создать функцию-помощник
CREATE OR REPLACE FUNCTION public.is_user_parent_message(parent_id UUID, user_uuid UUID)
RETURNS BOOLEAN
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $$
BEGIN
    RETURN EXISTS (
        SELECT 1 FROM public.support_messages 
        WHERE id = parent_id AND user_id = user_uuid
    );
END;
$$;

-- Удалить старые политики
DROP POLICY IF EXISTS "Users can view own messages" ON public.support_messages;
DROP POLICY IF EXISTS "Users can view own messages and admin replies" ON public.support_messages;

-- Создать новую политику БЕЗ рекурсии
CREATE POLICY "Users can view own messages and admin replies"
    ON public.support_messages
    FOR SELECT
    TO authenticated
    USING (
        user_id = auth.uid()
        OR (
            is_admin_reply = true 
            AND parent_message_id IS NOT NULL
            AND public.is_user_parent_message(parent_message_id, auth.uid())
        )
        OR LOWER(auth.jwt() ->> 'email') IN ('max.sokolvp@gmail.com', 'belovodvadim@gmail.com')
    );

-- Выдать права
GRANT EXECUTE ON FUNCTION public.is_user_parent_message(UUID, UUID) TO authenticated;
```

---

## ✅ Что будет работать

- ✅ Чат поддержки открывается
- ✅ Загружаются сообщения
- ✅ Можно отправлять новые сообщения
- ✅ Пользователи видят свои сообщения
- ✅ Пользователи видят ответы владельцев
- ✅ Владельцы видят всех пользователей
- ✅ Счётчик непрочитанных работает

---

## 🔍 Суть проблемы

**Было (❌ рекурсия):**
```sql
parent_message_id IN (
    SELECT id FROM support_messages WHERE ...
)
```

**Стало (✅ без рекурсии):**
```sql
is_user_parent_message(parent_message_id, auth.uid())
```

Функция `SECURITY DEFINER` обходит RLS и избегает рекурсии.

---

## 📚 Подробности

- **README_RECURSION_FIX.md** - полная инструкция
- **APPLY_RECURSION_FIX.md** - пошаговое руководство  
- **CHECKLIST_RECURSION_FIX.md** - чеклист проверки
- **test-support-messages-policies.sql** - тестовый скрипт

---

## ⏱️ Время: 2 минуты
## 🎯 Результат: Чат работает!

---

**ВАЖНО:** Используйте только файл `fix-support-messages-recursion.sql`  
~~НЕ используйте `fix-support-messages-view-policy.sql` - он вызывает рекурсию~~
