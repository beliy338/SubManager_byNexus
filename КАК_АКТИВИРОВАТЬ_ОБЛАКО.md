# ☁️ Как активировать облачное сохранение (5 минут)

## Шаг 1: Откройте SQL редактор Supabase

Перейдите по ссылке:  
**https://app.supabase.com/project/echbyusirwoojodjhhzi/sql/new**

## Шаг 2: Скопируйте SQL код

Откройте файл `/supabase/migrations/001_initial_schema.sql` и скопируйте весь его содержимое.

**Или скопируйте прямо отсюда:**

<details>
<summary>👉 Нажмите чтобы раскрыть SQL код</summary>

```sql
-- Create subscriptions table
CREATE TABLE IF NOT EXISTS subscriptions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  name TEXT NOT NULL,
  category TEXT NOT NULL,
  price DECIMAL(10, 2) NOT NULL,
  billing_cycle TEXT NOT NULL CHECK (billing_cycle IN ('monthly', 'yearly')),
  next_billing DATE NOT NULL,
  status TEXT NOT NULL DEFAULT 'active',
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS user_settings (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL UNIQUE REFERENCES auth.users(id) ON DELETE CASCADE,
  language TEXT NOT NULL DEFAULT 'ru',
  currency TEXT NOT NULL DEFAULT 'RUB',
  font_size TEXT NOT NULL DEFAULT 'medium' CHECK (font_size IN ('small', 'medium', 'large')),
  theme TEXT NOT NULL DEFAULT 'dark' CHECK (theme IN ('dark', 'light')),
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

ALTER TABLE subscriptions ENABLE ROW LEVEL SECURITY;
ALTER TABLE user_settings ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view their own subscriptions"
  ON subscriptions FOR SELECT USING (auth.uid() = user_id);
CREATE POLICY "Users can insert their own subscriptions"
  ON subscriptions FOR INSERT WITH CHECK (auth.uid() = user_id);
CREATE POLICY "Users can update their own subscriptions"
  ON subscriptions FOR UPDATE USING (auth.uid() = user_id);
CREATE POLICY "Users can delete their own subscriptions"
  ON subscriptions FOR DELETE USING (auth.uid() = user_id);

CREATE POLICY "Users can view their own settings"
  ON user_settings FOR SELECT USING (auth.uid() = user_id);
CREATE POLICY "Users can insert their own settings"
  ON user_settings FOR INSERT WITH CHECK (auth.uid() = user_id);
CREATE POLICY "Users can update their own settings"
  ON user_settings FOR UPDATE USING (auth.uid() = user_id);

CREATE INDEX idx_subscriptions_user_id ON subscriptions(user_id);
CREATE INDEX idx_subscriptions_next_billing ON subscriptions(next_billing);
CREATE INDEX idx_user_settings_user_id ON user_settings(user_id);

CREATE OR REPLACE FUNCTION update_updated_at_column()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updated_at = NOW();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER update_subscriptions_updated_at
  BEFORE UPDATE ON subscriptions
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();

CREATE TRIGGER update_user_settings_updated_at
  BEFORE UPDATE ON user_settings
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
```

</details>

## Шаг 3: Вставьте и запустите

1. Вставьте скопированный SQL в редактор
2. Нажмите кнопку **"Run"** (или Ctrl/Cmd + Enter)
3. Дождитесь сообщения "Success"

## Шаг 4: Проверьте результат

Выполните этот запрос чтобы убедиться что таблицы созданы:

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public';
```

Должны увидеть:
- ✅ `subscriptions`
- ✅ `user_settings`

## Шаг 5: Готово! Теперь протестируйте

1. Откройте приложение SubManager
2. Зарегистрируйтесь или войдите
3. Добавьте подписку
4. В боковом меню должна появиться иконка **"Cloud Synced"** ☁️ (зеленая)

---

## ✅ Что дальше?

Теперь ваше приложение:
- ☁️ **Сохраняет данные в облаке** - больше не потеряются при очистке браузера
- 🔄 **Синхронизируется между устройствами** - войдите с телефона и увидите те же подписки
- 🔐 **Защищено** - только вы видите свои данные
- 💾 **Автоматически резервируется** - Supabase делает бэкапы

---

## 🆘 Возникли проблемы?

### Ошибка "relation subscriptions does not exist"
**Причина:** SQL миграция не выполнена  
**Решение:** Повторите шаги 1-3

### В боковом меню "Local Storage" вместо "Cloud Synced"
**Причина:** Используется демо-аккаунт или миграция не применена  
**Решение:** 
1. Проверьте что SQL выполнен успешно
2. Выйдите и войдите заново
3. Если используете demo@submanager.app - создайте реальный аккаунт

### Подписки не синхронизируются
**Причина:** Политики безопасности не созданы  
**Решение:** Убедитесь что выполнили ВСЮ SQL миграцию полностью

---

## 📚 Дополнительная информация

- **Подробная инструкция:** `/supabase/SETUP_DATABASE.md`
- **Полное описание:** `/ОНЛАЙН_СОХРАНЕНИЕ.md`
- **Changelog:** `/CHANGELOG_CLOUD_SYNC.md`

---

**Удачи! Теперь ваши подписки всегда с вами! 🎉**
